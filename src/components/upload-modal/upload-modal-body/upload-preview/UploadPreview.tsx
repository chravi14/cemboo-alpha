import React from "react";

import * as Styled from "./UploadPreview.styled";

export const UploadPreview: React.FC = () => {
  return (
    <Styled.VideoWrapper>
      <Styled.VideoTitle>Video uploaded</Styled.VideoTitle>
    </Styled.VideoWrapper>
  );
};
